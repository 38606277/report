<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="function">

	<!--查我函数名称-->
	<select id="getFunctionName" parameterType="Map" resultType="Map">

		SELECT
			fn.func_class ,
			fn.func_name ,
			fn.func_id
		FROM
			func_name fn
		WHERE
			fn.func_name LIKE '%${name}%'
    </select>

	<!--查我函数参数-->
	<select id="getFunctionIn" parameterType="Map" resultType="Map">

		SELECT
		fn.func_class ,
		fn.func_name ,
		fn.func_id ,
		fi.param_id ,
		fi.param_name ,
		fi.dict ,
		fi.datatype
		FROM
		func_name fn ,
		func_in fi
		WHERE
		fn.func_id = fi.func_id
		AND fn.func_id = #{func_id}
	</select>

</mapper>
